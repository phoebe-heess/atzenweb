Read SESSION.md. Then AGENTS.md for full project context.

Two terminals:
1. `vercel dev --listen 3001`
2. `npm run dev`

Admin: http://localhost:3000/#admin | key: atzengold-admin-dev-key

Verify: `npx tsc --noEmit && npx vitest run && npm run build`

Branch: `main` (fast-forward from `code-quality-improvements`). Do not modify files without asking first.
